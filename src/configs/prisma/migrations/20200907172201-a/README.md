# Migration `20200907172201-a`

This migration has been generated by Joao Gabriel at 9/7/2020, 2:22:01 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."LineStationOrder" DROP CONSTRAINT "LineStationOrder_lineId_fkey"

ALTER TABLE "public"."LineStationOrder" DROP CONSTRAINT "LineStationOrder_stationId_fkey"

CREATE TABLE "public"."_LineToStation" (
"A" text   NOT NULL ,
"B" text   NOT NULL 
)

CREATE UNIQUE INDEX "_LineToStation_AB_unique" ON "public"."_LineToStation"("A", "B")

CREATE INDEX "_LineToStation_B_index" ON "public"."_LineToStation"("B")

ALTER TABLE "public"."_LineToStation" ADD FOREIGN KEY ("A")REFERENCES "public"."Line"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_LineToStation" ADD FOREIGN KEY ("B")REFERENCES "public"."Station"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."LineStationOrder"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200907171808-add-link-table..20200907172201-a
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -15,17 +15,17 @@
   updatedAt  DateTime  @default(now()) @updatedAt
 }
 model Line {
-  id         String     @id
-  number     Int        @unique
-  name       String     @unique
+  id         String    @id
+  number     Int       @unique
+  name       String    @unique
   color      String
-  active     Boolean?   @default(true)
-  stations   LineStationOrder[]
+  active     Boolean?  @default(true)
+  stations   Station[]
   companyId  String
-  createdAt  DateTime   @default(now())
-  updatedAt  DateTime   @default(now()) @updatedAt
+  createdAt  DateTime  @default(now())
+  updatedAt  DateTime  @default(now()) @updatedAt
   company    Company    @relation(fields: [companyId], references: [id])
 }
@@ -33,17 +33,8 @@
   id           String    @id
   displayName  String
   fullName     String    @unique
   elevator     Boolean?  @default(false)
+  lines        Line[]  
   createdAt    DateTime  @default(now())
   updatedAt    DateTime  @default(now()) @updatedAt
-}
-
-model LineStationOrder {
-  id         String   @id
-  order      Int
-  lineId     String
-  stationId  String
-
-  line       Line     @relation(fields: [lineId], references: [id])
-  station    Station  @relation(fields: [stationId], references: [id])
 }
```


