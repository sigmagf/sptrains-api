# Migration `20200907171433-initial`

This migration has been generated by Joao Gabriel at 9/7/2020, 2:14:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Company" (
"id" text   NOT NULL ,
"name" text   NOT NULL ,
"alias" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Line" (
"id" text   NOT NULL ,
"number" integer   NOT NULL ,
"name" text   NOT NULL ,
"color" text   NOT NULL ,
"active" boolean   DEFAULT true,
"companyId" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Station" (
"id" text   NOT NULL ,
"displayName" text   NOT NULL ,
"fullName" text   NOT NULL ,
"elevator" boolean   DEFAULT false,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."_LineToStation" (
"A" text   NOT NULL ,
"B" text   NOT NULL 
)

CREATE UNIQUE INDEX "Line.number_unique" ON "public"."Line"("number")

CREATE UNIQUE INDEX "Line.name_unique" ON "public"."Line"("name")

CREATE UNIQUE INDEX "Station.fullName_unique" ON "public"."Station"("fullName")

CREATE UNIQUE INDEX "_LineToStation_AB_unique" ON "public"."_LineToStation"("A", "B")

CREATE INDEX "_LineToStation_B_index" ON "public"."_LineToStation"("B")

ALTER TABLE "public"."Line" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_LineToStation" ADD FOREIGN KEY ("A")REFERENCES "public"."Line"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_LineToStation" ADD FOREIGN KEY ("B")REFERENCES "public"."Station"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200907171433-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,40 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Company {
+  id         String    @id
+  name       String
+  alias      String
+  createdAt  DateTime  @default(now())
+  updatedAt  DateTime  @default(now()) @updatedAt
+}
+
+model Line {
+  id         String     @id
+  number     Int        @unique
+  name       String     @unique
+  color      String
+  active     Boolean?   @default(true)
+  stations   Station[]
+  companyId  String
+  createdAt  DateTime   @default(now())
+  updatedAt  DateTime   @default(now()) @updatedAt
+
+  company    Company    @relation(fields: [companyId], references: [id])
+}
+
+model Station {
+  id           String    @id
+  displayName  String
+  fullName     String    @unique
+  elevator     Boolean?  @default(false)
+  lines        Line[]
+  createdAt    DateTime  @default(now())
+  updatedAt    DateTime  @default(now()) @updatedAt
+}
```


