# Migration `20200912233522-operators-to-operator`

This migration has been generated by Joao Gabriel at 9/12/2020, 8:35:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200908234823-nao-me-fodi-e-adicionei-detalhes-para-a-ligacao-estacao-linha..20200912233522-operators-to-operator
--- datamodel.dml
+++ datamodel.dml
@@ -1,14 +1,14 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
-model Operators {
+model Operator {
   id         String    @id @default(uuid())
   name       String
   alias      String
   lines      Line[]
@@ -18,48 +18,48 @@
   @@map("operators")
 }
 model Line {
-  id          String    @id @default(uuid())
-  number      Int       @unique
-  name        String    @unique
+  id          String            @id @default(uuid())
+  number      Int               @unique
+  name        String            @unique
   color       String
-  active      Boolean?  @default(true)
+  active      Boolean?          @default(true)
   stations    StationsOnLine[]
   operatorId  String
-  createdAt   DateTime  @default(now())
-  updatedAt   DateTime  @default(now()) @updatedAt
+  createdAt   DateTime          @default(now())
+  updatedAt   DateTime          @default(now()) @updatedAt
-  operator    Operators    @relation(fields: [operatorId], references: [id])
+  operator    Operator          @relation(fields: [operatorId], references: [id])
   @@map("lines")
 }
 model Station {
-  id           String    @id @default(uuid())
+  id           String            @id @default(uuid())
   displayName  String
-  fullName     String    @unique
-  elevator     Boolean?  @default(false)
+  fullName     String            @unique
+  elevator     Boolean?          @default(false)
   lines        StationsOnLine[]
-  createdAt    DateTime  @default(now())
-  updatedAt    DateTime  @default(now()) @updatedAt
+  createdAt    DateTime          @default(now())
+  updatedAt    DateTime          @default(now()) @updatedAt
   @@map("stations")
 }
 model StationsOnLine {
   @@id([lineId, stationId])
-  id          String   @unique @default(uuid())
+  id          String           @unique @default(uuid())
   lineId      String
   stationId   String
   nextId      String?
   details     String?
-  line        Line     @relation(fields: [lineId], references: [id])
-  station     Station  @relation(fields: [stationId], references: [id])
+  line        Line             @relation(fields: [lineId], references: [id])
+  station     Station          @relation(fields: [stationId], references: [id])
-  next        StationsOnLine? @relation("line-station-link", fields: [nextId], references: [id])
-  previous    StationsOnLine? @relation("line-station-link")
-
+  next        StationsOnLine?  @relation("line-station-link", fields: [nextId], references: [id])
+  previous    StationsOnLine?  @relation("line-station-link")
+  
   @@map("_stationsOnLine")
 }
```


